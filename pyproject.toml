[project]
name = "comic-library"
version = "0.1.0"
dependencies = []

[tool.setuptools]
packages = ["classes", "tagging"]

[tool.ruff.lint]
extend-select = ["I", "B"]

[tool.safety]
policy_file = "none"
full_report = true
output = "json"

[tool.mypy]
python_version = "3.13"
